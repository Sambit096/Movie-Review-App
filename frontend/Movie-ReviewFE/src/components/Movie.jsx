import { useParams } from "react-router-dom"

const Movie = () => {

    const { id } = useParams()

    // API Fetch to get this specific Movie info

    return (
        <h1>TODO</h1>
    )
}

export default Movie